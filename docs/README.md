Put screenshots here so the main README renders them:

- `tonight.png` — the Tonight page with a pick and its reasons
- `library.png` — the library grid with the context rail
- `game.png` — a game page with hero art and media
- `profile.png` — the profile page with the personality card

Windows: Win+Shift+S to snip, then save with these names. Aim for a browser
window around 1400px wide so the text is readable in GitHub's layout.
